const vehicle = (Array.isArray(b.vehicle) ? b.vehicle[0] : b.vehicle) as { ... } | null
const renter  = (Array.isArray(b.renter)  ? b.renter[0]  : b.renter)  as { ... } | null
const host    = (Array.isArray(b.host)    ? b.host[0]    : b.host)    as { ... } | null
